# Auth 관련 Util 개발

## 목표

Auth 관련 유틸을 개발하여, 인증이 필요한 종단 관심사들에 개발을 편하게 한다.

## 단계

1. AuthInfo 객체와 AuthInfoAffect annotation을 선언한다 (주요 인터페이스가 될 예정)
2. annotion의 param에 따라 인증되지 않은 유저 차단기능을 개발한다.
3. annotion의 param에 따라 AuthInfo를 주입해주는 기능을 개발한다.
4. annotion이 달린 객체에 대해 Swagger에서 인증 기능을 보여주도록한다.

## 완료 AI 피드백

### ✅ 잘한 점

1. **깔끔한 인터페이스 설계**
   - `AuthInfo` 데이터 클래스로 인증 정보를 명확하게 표현
   - `AuthInfoAffect` 어노테이션으로 선언적 인증 처리 가능
   - `required` 파라미터로 필수/선택적 인증을 유연하게 지원

2. **Spring 표준 패턴 활용**
   - `HandlerMethodArgumentResolver`를 사용하여 Spring의 표준 메커니즘 활용
   - `WebMvcConfigurer`를 통한 설정으로 프레임워크와 자연스럽게 통합
   - ArgumentResolver를 우선순위 0으로 등록하여 validation 전에 처리하도록 설계

3. **메소드/클래스 레벨 어노테이션 지원**
   - 메소드 레벨 어노테이션이 클래스 레벨보다 우선순위가 높도록 구현
   - 유연한 적용 범위로 코드 중복 최소화

4. **의존성 역전 원칙 적용**
   - `AuthTokenResolver` 인터페이스를 통해 구체적인 구현과 분리
   - 테스트 용이성과 확장성 향상

5. **Swagger 통합 완성도**
   - `OperationCustomizer`를 통해 자동으로 Security 요구사항 추가
   - AuthInfo 파라미터를 Swagger 문서에서 자동 제거하여 사용자 경험 개선
   - Bearer Token 스킴을 명확하게 설정

6. **명확한 문서화**
   - KDoc 주석으로 각 컴포넌트의 역할과 사용법 설명
   - 예제 코드 포함으로 이해도 향상

7. **에러 처리**
   - `DomainException`을 통한 일관된 에러 처리
   - `required`에 따른 적절한 예외 발생 또는 null 반환

### ⚠️ 개선할 점

1. **에러 메시지 일관성**
   - 일부 에러 메시지가 영어로 되어 있음 (`"Authorization header is missing"`)
   - **반박**: 일단 영어로 통일할 예정. 추후 국제화(i18n) 시 변경 예정

1. **OpenApiConfig 복잡도**
   - 파라미터 제거 로직이 다소 복잡함 (이름 기반 → 인덱스 기반 fallback)
   - `SpringDocUtils.getConfig().addRequestWrapperToIgnore()` 같은 전역 설정 활용 검토 가능

1. **테스트 코드 부재**
   - `AuthInfoArgumentResolver`에 대한 단위 테스트 필요
   - `OpenApiConfig`의 OperationCustomizer 동작 검증 필요
   - 통합 테스트로 전체 플로우 검증 권장

1. **null 안전성**
   - Kotlin의 null 안전성 활용을 더 강화할 수 있음
   - `AuthInfo?`와 `AuthInfo` 타입 구분을 더 명확하게 처리

1. **성능 고려사항**
   - 어노테이션 체크가 매 요청마다 발생하므로, 캐싱 전략 고려 가능
   - 하지만 현재 구현도 충분히 효율적임

### 💬 반박 및 추가 설명

1. **에러 메시지 언어 정책**
   - 현재 영어로 통일하는 것이 의도된 설계입니다
   - 추후 국제화(i18n) 지원 시 다국어 메시지로 전환 예정
   - API 에러 메시지는 영어로 통일하는 것이 일반적인 관례입니다

2. **OpenApiConfig 복잡도**
   - 파라미터 제거 로직의 복잡도는 Swagger의 제약사항 때문입니다
   - 파라미터 이름을 가져올 수 없는 경우를 대비한 fallback 로직이 필요합니다
   - 현재 구현은 안정성과 호환성을 모두 고려한 결과입니다

3. **테스트 코드**
   - E2E 테스트에서 이미 인증 플로우가 검증되고 있습니다 (`AuthControllerE2ETest`, `UserControllerE2ETest`)
   - 컴포넌트 단위 테스트도 존재합니다 (`AccessTokenVerifierTest`, `AccessTokenIssuerTest`, `RefreshTokenIssuerTest`)
   - 현재는 통합 테스트와 컴포넌트 단위 테스트로 충분히 검증 가능합니다

4. **null 안전성**
   - Kotlin의 null 안전성은 이미 적절히 활용하고 있습니다
   - `AuthInfo?`와 `AuthInfo` 타입 구분은 `required` 파라미터로 명확하게 처리됩니다
   - 추가적인 타입 구분은 오히려 복잡도만 증가시킬 수 있습니다

5. **성능 최적화**
   - 어노테이션 체크는 Spring의 리플렉션 메커니즘을 사용하므로 매우 빠릅니다
   - 현재 트래픽 수준에서는 캐싱이 오히려 오버헤드가 될 수 있습니다
   - **의도**: 직접 성능 병목을 경험하고 최적화를 수행해보고 싶어서 의도적으로 여지를 남겨두었습니다

### 📝 종합 평가

**전체적으로 매우 잘 구현된 기능입니다!** 

목표했던 4가지 단계를 모두 완료했으며, Spring의 표준 패턴을 잘 활용하고 있습니다. 특히 Swagger 통합 부분이 인상적입니다. 몇 가지 작은 개선점들이 있지만, 전반적으로 프로덕션에 사용 가능한 수준의 코드입니다.

**추가 권장사항:**
- 테스트 코드 작성으로 안정성 확보
- 문서 오타 수정
- 에러 메시지 일관성 개선
